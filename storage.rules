rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Receipts: only the owning user can read/write their own files
    match /receipts/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}